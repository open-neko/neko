# Dev overlay for the embedded release stack. The source hot-reload workflow is
# repo-root `pnpm dev:setup` + `pnpm dev`: Docker runs the dependency/demo data
# services, while web/worker run as host source processes. Keep this overlay
# empty until we add Docker-only dev tooling that belongs in the release CLI.
services: {}
