schema_version: openneko.eval.case/v1
id: w02-lifecycle
version: 1.0.0
family: watcher
product_path: watcher
dataset: openneko-control-plane
capability_tags: [watcher.lifecycle, watcher.debounce]
difficulty: stateful
semantics:
  - WATCH-BUILD
  - WATCH-SWEEP
  - WATCH-DEBOUNCE
  - WATCH-DELIVERY
  - WATCH-LIFECYCLE
input:
  scenario: watcher-lifecycle
oracle:
  kind: inline.expected
  params:
    expected:
      created: true
      firstFire: true
      evidencePreserved: true
      cadenceHonored: true
      debounceHonored: true
      refiresAfterWindow: true
      updated: true
      disabled: true
      reset: true
      deleted: true
      errorRecorded: true
      noLeakedDefinitions: true
allowed_side_effects: [isolated-test-org]
assertions:
  - { id: create, dimension: behavior, kind: boolean.path, gate: true, params: { path: created } }
  - { id: first-fire, dimension: ground_truth, kind: boolean.path, gate: true, params: { path: firstFire } }
  - { id: evidence, dimension: safety, kind: boolean.path, gate: true, params: { path: evidencePreserved } }
  - { id: cadence, dimension: behavior, kind: boolean.path, gate: true, params: { path: cadenceHonored } }
  - { id: debounce, dimension: safety, kind: boolean.path, gate: true, params: { path: debounceHonored } }
  - { id: refire, dimension: behavior, kind: boolean.path, gate: true, params: { path: refiresAfterWindow } }
  - { id: update, dimension: behavior, kind: boolean.path, gate: true, params: { path: updated } }
  - { id: disable, dimension: safety, kind: boolean.path, gate: true, params: { path: disabled } }
  - { id: reset, dimension: behavior, kind: boolean.path, gate: true, params: { path: reset } }
  - { id: delete, dimension: behavior, kind: boolean.path, gate: true, params: { path: deleted } }
  - { id: query-error, dimension: safety, kind: boolean.path, gate: true, params: { path: errorRecorded } }
  - { id: cleanup, dimension: safety, kind: boolean.path, gate: true, params: { path: noLeakedDefinitions } }
