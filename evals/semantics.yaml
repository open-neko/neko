schema_version: openneko.eval.semantics/v1
generated_from: evals/SEMANTICS.md
entries:
  - id: RUNTIME-INGRESS
    category: runtime-backend
    description: Web, channel, job, and workflow ingress create the correct
      org/user/service principal, thread, and run
    verification: E2E DB state + trace
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-HERMES-CONTRACT
    category: runtime-backend
    description: Hermes implements the agent event, tool, sandbox, and provider contract
    verification: contract tests + live provider E2E
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-BACKEND-NATIVE
    category: runtime-backend
    description: Backend-native delegation, session resume, permission hooks, and
      MCP behavior work when advertised
    verification: capability-specific E2E
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-MODEL-RESOLUTION
    category: runtime-backend
    description: Requested provider/model/credential resolve to the actual backend
      configuration without secret exposure
    verification: contract + telemetry
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-STREAM
    category: runtime-backend
    description: Messages, tool lifecycle, status, cards, artifacts, and completion
      stream in causal order
    verification: event-sequence oracle
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-CANCEL
    category: runtime-backend
    description: Cancellation stops the active backend/sandbox and persists a
      cancelled terminal state
    verification: state + process oracle
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-TIMEOUT
    category: runtime-backend
    description: Turn and provider timeouts are classified accurately and terminate
      descendants
    verification: fault injection
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-RETRY
    category: runtime-backend
    description: Only retryable failures retry; attempts, usage, and final failure
      classification remain accurate
    verification: attempt journal oracle
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-RESUME
    category: runtime-backend
    description: Thread/backend session resume preserves intended context without
      re-injecting truncated turns
    verification: multi-turn differential
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-VALIDATE
    category: runtime-backend
    description: Invalid structured output is repaired within bounds or fails honestly
    verification: schema + attempt oracle
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-CONCURRENCY
    category: runtime-backend
    description: Global/backend caps, queueing, cancellation, and fairness hold
      under parallel work
    verification: load/soak
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-SHUTDOWN
    category: runtime-backend
    description: Process shutdown cancels agents and leaves jobs/runs recoverable
    verification: restart smoke
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-SANDBOX
    category: runtime-backend
    description: Every production agent runs in the required OpenShell boundary with
      declared mounts and capabilities
    verification: containment E2E
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: RUNTIME-OBSERVABILITY
    category: runtime-backend
    description: Canonical observations, OTel spans/metrics, and run summaries agree
      and never alter outcomes
    verification: telemetry conformance
    disposition: declared
    owners:
      - packages/llm/src/agent-backend.ts
      - apps/worker/src
  - id: DATA-SOURCE-SELECT
    category: data-calculation
    description: The correct enabled/default source is selected; disabled or
      ambiguous sources are handled explicitly
    verification: registry/state oracle
    disposition: declared
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-SOURCE-ADMIN
    category: data-calculation
    description: Register/preview/apply/enable/disable/default-source changes follow
      admin approval and preserve credentials
    verification: state + approval oracle
    disposition: declared
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-SOURCE-FILE
    category: data-calculation
    description: File-backed source ingestion, config generation, and refresh expose
      the intended data
    verification: dataset pack E2E
    disposition: declared
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-SOURCE-OPENAPI
    category: data-calculation
    description: Managed OpenAPI ingestion/configuration exposes allowed reads and
      explicitly opted-in API `call` mutations without overgranting
    verification: fake-server E2E
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-DISCOVERY
    category: data-calculation
    description: Catalog/schema discovery finds relevant tables, columns,
      relationships, and query patterns
    verification: answer + trace method
    disposition: declared
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-KNOWLEDGE
    category: data-calculation
    description: Knowledge-pack prefetch, freshness, fallback, and agentic catalog
      modes are correct
    verification: digest + fault injection
    disposition: declared
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-DIRECT
    category: data-calculation
    description: Direct GraphJin execution returns grounded read-only results
      through the brokered path
    verification: runtime oracle
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-DELEGATED
    category: data-calculation
    description: Delegated GraphJin agent results carry sufficient
      data/evidence/usage and obey read-only policy
    verification: outer+inner trace + oracle
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-PLANNER-EXECUTE
    category: data-calculation
    description: A delegated planner may propose, but the trusted host executes and
      verifies the final query
    verification: plan/query/oracle
    disposition: declared
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-MULTISOURCE
    category: data-calculation
    description: One answer can select and reconcile several registered sources
      without confusing identities or dates
    verification: cross-source oracle
    disposition: declared
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-AUTH
    category: data-calculation
    description: GraphJin tokens carry the correct org/user/role; row and source
      restrictions are enforced
    verification: cross-principal negative E2E
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-QUERY-GUARD
    category: data-calculation
    description: "GraphJin applies source-aware policy: read-only targets reject
      writes while explicitly exposed API `call` mutations remain executable"
    verification: adversarial contract + fake-server E2E
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-SCHEMA-DRIFT
    category: data-calculation
    description: Catalog refresh and repair recover from supported schema changes
      and fail honestly otherwise
    verification: drift fixture
    disposition: declared
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: DATA-PAGINATION
    category: data-calculation
    description: Answers do not treat a truncated page as a complete dataset
    verification: method + ground truth
    disposition: declared
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: CALC-SCALAR
    category: data-calculation
    description: Exact counts, sums, averages, distinct counts, ratios, and unit
      conversions match ground truth
    verification: numeric oracle
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: CALC-FILTER
    category: data-calculation
    description: Boolean, enum, null, text, and compound filters match ground truth
    verification: row/value oracle
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: CALC-TIME
    category: data-calculation
    description: Latest-data anchors, inclusive/exclusive windows, fiscal periods,
      timezones, and comparison periods are correct
    verification: value + interval oracle
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: CALC-JOIN
    category: data-calculation
    description: Header/detail and multi-hop joins avoid fan-out, omissions, and
      duplicate aggregation
    verification: value + method oracle
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: CALC-RANK
    category: data-calculation
    description: Top/bottom grouped rankings return the correct value and dimension
      with database-side grouping
    verification: value/dimension + method
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: CALC-SERIES
    category: data-calculation
    description: Ordered time series and chart grains contain correct points, gaps,
      and comparison values
    verification: ordered-series oracle
    disposition: declared
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: CALC-BUSINESS
    category: data-calculation
    description: Multi-step domain calculations preserve definitions, units, and assumptions
    verification: composite oracle
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: CALC-EMPTY
    category: data-calculation
    description: Empty, missing, null, and zero are distinguished without invention
    verification: structured oracle
    disposition: declared
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: CALC-EVIDENCE
    category: data-calculation
    description: Headline, structured data, explanation, source, freshness, and
      evidence agree
    verification: cross-field scorer
    disposition: eval
    owners:
      - packages/llm/src/graphjin
      - packages/llm/src/work
  - id: ONBOARD-PROFILE
    category: product-output
    description: Business profiling is grounded in the connected source and produces
      a valid editable profile
    verification: fixture assertions + contract
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: ONBOARD-RESEARCH
    category: product-output
    description: Optional industry research runs only when configured and remains
      distinct from company facts
    verification: provider fake + provenance
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: ONBOARD-BOOTSTRAP
    category: product-output
    description: Bootstrap metrics fit selected seats, company data, role
      priorities, and required count/shape
    verification: deterministic + rubric
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: METRIC-REFRESH
    category: product-output
    description: Metric refresh computes/validates/persists the correct snapshot and
      status on success/failure
    verification: DB + ground-truth oracle
    disposition: eval
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: BRIEFING-BUILD
    category: product-output
    description: Briefing composition selects fresh, relevant metrics/findings with
      correct deep-dive context
    verification: DB + rendering contract
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: OUTPUT-MESSAGE
    category: product-output
    description: User-visible prose excludes hidden fences, raw tool calls, and
      backend protocol debris
    verification: exact content contract
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: OUTPUT-SURFACE
    category: product-output
    description: A2UI/card payloads validate and remain separate from prose
    verification: schema + event oracle
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: OUTPUT-VITALS
    category: product-output
    description: Headline vitals contain observed/calculated/estimated basis,
      freshness, and source when available
    verification: structured scorer
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: OUTPUT-FOLLOWUPS
    category: product-output
    description: Suggested follow-ups are valid, useful, and emitted once
    verification: event/contract scorer
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: OUTPUT-ARTIFACT
    category: product-output
    description: Generated artifacts are present, safely named, downloadable, and
      scoped to the run
    verification: file digest + auth oracle
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: OUTPUT-NEEDS-INPUT
    category: product-output
    description: Genuine ambiguity asks a bounded question without pretending completion
    verification: event-sequence scorer
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: OUTPUT-VALUE
    category: product-output
    description: Analysis/action time-saved estimates are parsed, clamped,
      attributed, and not double-counted
    verification: DB state oracle
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: OUTPUT-CHANNEL-FIDELITY
    category: product-output
    description: The same channel-neutral answer degrades correctly to web, Slack,
      Telegram, and voice capabilities
    verification: projection golden tests
    disposition: declared
    owners:
      - packages/llm/src
      - apps/worker/src/jobs
  - id: WORK-THREAD
    category: work-context
    description: Thread/run/message/event persistence, ordering, deletion,
      truncation, and replay are correct
    verification: DB state machine
    disposition: eval
    owners:
      - packages/llm/src/work
  - id: WORK-CONTEXT-BRIEFING
    category: work-context
    description: A briefing deep dive carries the intended metric/finding context
    verification: prompt + answer oracle
    disposition: declared
    owners:
      - packages/llm/src/work
  - id: WORK-CONTEXT-APP
    category: work-context
    description: App and record context limits tools/data to the selected app/record
    verification: auth + tool inventory
    disposition: declared
    owners:
      - packages/llm/src/work
  - id: WORK-MEMORY-SEARCH
    category: work-context
    description: Semantic memory retrieves relevant allowed memories and reports
      misses honestly
    verification: seeded retrieval eval
    disposition: eval
    owners:
      - packages/llm/src/work
  - id: WORK-MEMORY-WRITE
    category: work-context
    description: Agent/fence-driven memory writes validate scope, attribution,
      confidence, and dedupe
    verification: state oracle
    disposition: eval
    owners:
      - packages/llm/src/work
  - id: WORK-MEMORY-LAYERS
    category: work-context
    description: Personal/team overlays, hide/correct/promote/adopt, fork/pull, and
      restore preserve lineage
    verification: versioned state oracle
    disposition: declared
    owners:
      - packages/llm/src/work
  - id: WORK-MEMORY-INTEGRITY
    category: work-context
    description: Seal verification, expiry/TTL, and tamper handling prevent poisoned context
    verification: adversarial state test
    disposition: declared
    owners:
      - packages/llm/src/work
  - id: LIBRARY-DISTILL
    category: work-context
    description: Uploaded documents triage, extract, and distill into OKF concepts
      on the uploader's personal layer — update-not-append, content-hash dedupe,
      forced retry, clean failure reasons
    verification: distiller fixture oracle
    disposition: declared
    owners:
      - packages/llm/src/work
  - id: LIBRARY-LAYERS
    category: work-context
    description: Library concepts honor personal/team layering,
      share/approve/deprecate transitions, staleness sweep, and bundle
      export/import round-trip with trust state intact
    verification: versioned state oracle
    disposition: declared
    owners:
      - packages/llm/src/work
  - id: WORK-LIBRARY-SEARCH
    category: work-context
    description: The agent retrieves the current identity-scoped library concept,
      rejects stale or adversarial decoys, and applies the retrieved procedure
      to its answer
    verification: planted retrieval + answer + trusted trace
    disposition: eval
    owners:
      - packages/llm/src/work
  - id: WORK-WORKFLOW-RETRIEVE
    category: work-context
    description: The agent lists saved workflows, selects the intended definition
      rather than a similarly named decoy, and applies its exact goal or steps
    verification: planted workflow + answer + trusted trace
    disposition: eval
    owners:
      - packages/llm/src/work
  - id: WORK-PERSONA
    category: work-context
    description: Operator role/profile changes prompt emphasis without weakening
      authorization
    verification: matched persona cases
    disposition: declared
    owners:
      - packages/llm/src/work
  - id: WORK-COMPACTION
    category: work-context
    description: Long-thread compaction retains decisions, figures, actions, and
      open tasks without cross-thread leakage
    verification: multi-turn oracle
    disposition: eval
    owners:
      - packages/llm/src/work
  - id: WORK-SKILLS
    category: work-context
    description: Skill discovery, dependency aggregation, install/use, and
      allowed-tool implications are correct
    verification: trace + artifact contract
    disposition: eval
    owners:
      - packages/llm/src/work
  - id: WORK-CONFIG-VCS
    category: work-context
    description: Snapshot/history/restore/promote/adopt of config artifacts preserve
      semantic content and attribution
    verification: repository/DB oracle
    disposition: declared
    owners:
      - packages/llm/src/work
  - id: FLOW-BUILD
    category: workflow-automation
    description: Natural-language workflow creation/update/delete produces a valid
      definition with provenance
    verification: DB + schema oracle
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: FLOW-RULE-BUILD
    category: workflow-automation
    description: Approval-rule creation/update resolves scopes, risks, roles, and
      modes correctly
    verification: policy state oracle
    disposition: declared
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: FLOW-CRON
    category: workflow-automation
    description: Cron trigger creation, due selection, and sweep scheduling fire at
      the intended times once
    verification: fake clock + state
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: FLOW-SOURCE-CHANGE
    category: workflow-automation
    description: Matching source row changes trigger the correct workflow;
      non-matches do not
    verification: positive/negative event oracle
    disposition: declared
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: FLOW-SUBSCRIPTION
    category: workflow-automation
    description: GraphJin subscriptions are created/reconciled/deleted and route
      matches idempotently
    verification: lifecycle oracle
    disposition: declared
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: FLOW-EXTERNAL
    category: workflow-automation
    description: External-event filters, provenance, and dispatch select the correct
      workflows
    verification: event oracle
    disposition: declared
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: FLOW-RUN
    category: workflow-automation
    description: Workflow turns use the runner tool policy, persist state, and
      produce outputs/actions correctly
    verification: state + trace oracle
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: FLOW-CYCLE
    category: workflow-automation
    description: Chain depth, cycle detection, run budget, and fan-out caps stop
      runaway graphs
    verification: adversarial graph fixture
    disposition: declared
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: FLOW-OUTPUT
    category: workflow-automation
    description: Output persistence, evidence, delivery hooks, and source-write
      attribution are correct
    verification: DB/event oracle
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: FLOW-OUTPUT-DEDUPE
    category: workflow-automation
    description: Repeated findings collapse according to semantic identity and
      occurrence count
    verification: repeated-event oracle
    disposition: declared
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: FLOW-OUTPUT-MUTE
    category: workflow-automation
    description: Muted scopes suppress presentation without deleting underlying evidence
    verification: DB + query oracle
    disposition: declared
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: FLOW-OUTPUT-TTL
    category: workflow-automation
    description: Stale outputs expire without removing protected/pinned state incorrectly
    verification: fake clock + DB
    disposition: declared
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: WATCH-BUILD
    category: workflow-automation
    description: Natural-language watcher creation yields a valid query, value path,
      operator, threshold, cadence, and severity
    verification: state + query oracle
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: WATCH-CONDITION
    category: workflow-automation
    description: "`gt/gte/lt/lte/eq/ne/changed` conditions handle types and prior
      values correctly"
    verification: deterministic matrix
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: WATCH-SWEEP
    category: workflow-automation
    description: Due scheduling, query execution, error handling, and
      last-value/check timestamps are correct
    verification: fake clock + DB
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: WATCH-DEBOUNCE
    category: workflow-automation
    description: A tripped watch fires once within debounce and may fire again after
      the window
    verification: event sequence
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: WATCH-DELIVERY
    category: workflow-automation
    description: A watch event reaches the linked workflow/output/action path with
      original evidence
    verification: causal trace + state
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: WATCH-LIFECYCLE
    category: workflow-automation
    description: Create/enable/disable/update/delete/reset and cleanup do not leak
      subscriptions or events
    verification: stateful E2E
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: ACTION-PROPOSE
    category: workflow-automation
    description: The harness creates the intended internal/external action with
      target, payload, risk, actor, and rationale
    verification: request state oracle
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: ACTION-POLICY
    category: workflow-automation
    description: Policy precedence, condition match, auto-approve/pending/deny, and
      role requirements are correct
    verification: policy decision matrix
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: ACTION-APPROVAL
    category: workflow-automation
    description: Approve/reject transitions enforce actor permissions and prevent
      invalid/repeated decisions
    verification: state-machine oracle
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: ACTION-EXECUTE
    category: workflow-automation
    description: Approved actions execute through the correct adapter and persist
      success/failure receipts
    verification: fake adapter + DB
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: ACTION-IDEMPOTENCY
    category: workflow-automation
    description: Retries, duplicate jobs, and repeated approvals cause at most one
      external effect
    verification: receipt/external fake
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: ACTION-COLLATERAL
    category: workflow-automation
    description: The intended mutation occurs and unrelated records/resources remain
      unchanged
    verification: post-state diff
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: ACTION-ADAPTER
    category: workflow-automation
    description: Code, webhook, and plugin adapters validate payloads, respect
      egress, and normalize outcomes
    verification: adapter contract/E2E
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: ACTION-AUDIT
    category: workflow-automation
    description: Proposal, policy, approval, execution, rejection, and failure are
      attributable and tamper-evident
    verification: audit-chain oracle
    disposition: eval
    owners:
      - packages/llm/src/workflows
      - apps/worker/src/jobs
  - id: CHANNEL-INBOUND
    category: channels-plugins
    description: Poll/webhook/socket ingestion normalizes messages and button actions once
    verification: adapter + dedupe oracle
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: CHANNEL-WORKSPACE-ROUTE
    category: channels-plugins
    description: External workspace/conversation maps to the correct org and
      persistent channel thread
    verification: DB routing oracle
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: CHANNEL-IDENTITY
    category: channels-plugins
    description: Sender linking by explicit approval or verified identity assigns
      the correct user/role; strangers stay restricted
    verification: cross-identity E2E
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: CHANNEL-DELIVERY
    category: channels-plugins
    description: Output messages, cards/degradation, receipts, errors, and
      follow-ups deliver to the originating channel
    verification: fake channel transcript
    disposition: eval
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: CHANNEL-BACKOFF
    category: channels-plugins
    description: Poll failures back off without duplicate delivery or log/run floods
    verification: fake clock/load
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: ADMIN-USER
    category: channels-plugins
    description: Invite, role change, deactivate/reactivate, and authorization
      require the correct approval
    verification: control-plane state oracle
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: ADMIN-SOURCE
    category: channels-plugins
    description: Source registry/config/role changes use secure forms for secrets
      and preserve preview/apply semantics
    verification: state + no-secret oracle
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: ADMIN-CHANNEL
    category: channels-plugins
    description: Workspace/member/link administration is scoped, attributable, and
      approval-gated
    verification: state + policy oracle
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: ADMIN-PLUGIN
    category: channels-plugins
    description: Plugin list/install/remove/update surfaces permissions and enforces
      install policy
    verification: registry + approval oracle
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: PLUGIN-MANIFEST
    category: channels-plugins
    description: Manifest validation and declared capabilities/egress match runtime
      registration
    verification: contract test
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: PLUGIN-SANDBOX
    category: channels-plugins
    description: Each plugin receives only declared mounts, credentials, and network
      destinations
    verification: containment E2E
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: PLUGIN-HOT-RELOAD
    category: channels-plugins
    description: Install/remove/secret rotation reconciles the live registry without
      process restart
    verification: lifecycle E2E
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: PLUGIN-ACTION
    category: channels-plugins
    description: Plugin actions become correctly described agent tools and execute
      only through action policy
    verification: tool + fake adapter
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: PLUGIN-CONNECT
    category: channels-plugins
    description: Connector discovery, per-user OAuth/token refresh, and source sync
      remain user-scoped
    verification: fake OAuth + sync oracle
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: PLUGIN-CHANNEL
    category: channels-plugins
    description: Channel capabilities register and drive inbound/outbound through
      the common adapter
    verification: plugin contract
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: PLUGIN-AUTH
    category: channels-plugins
    description: Singleton auth capability, sign-in, group/role mapping, and session
      gating are correct
    verification: fake OIDC + DB
    disposition: declared
    owners:
      - packages/channels
      - packages/plugin-types
      - packages/plugin-install
  - id: RECORDS-REGISTRY
    category: records
    description: App/object definitions, availability mirror, and catalog remain consistent
    verification: catalog/schema oracle
    disposition: declared
    owners:
      - packages/records
  - id: RECORDS-SCHEMA-PLAN
    category: records
    description: Schema proposals, previews, counterproposals, DDL saga, and audit
      are deterministic and recoverable
    verification: state-machine E2E
    disposition: eval
    owners:
      - packages/records
  - id: RECORDS-READ
    category: records
    description: List/detail/reference/aggregate queries honor filters, ordering,
      cursors, and projections
    verification: query/result oracle
    disposition: declared
    owners:
      - packages/records
  - id: RECORDS-WRITE
    category: records
    description: Create/update/delete/backfill validate fields/references and record
      change history
    verification: pre/post-state oracle
    disposition: declared
    owners:
      - packages/records
  - id: RECORDS-RECYCLE
    category: records
    description: Soft-deleted records are listed/restored/purged only with correct
      permission
    verification: state + auth oracle
    disposition: declared
    owners:
      - packages/records
  - id: RECORDS-POLICY
    category: records
    description: App/object/field/row permissions and actor synchronization match
      GraphJin enforcement
    verification: cross-role differential
    disposition: eval
    owners:
      - packages/records
  - id: RECORDS-IMPORT
    category: records
    description: CSV/artifact plan hash, sampling, batched execution, rejects,
      cancellation, resume, and receipt are correct
    verification: import fixture oracle
    disposition: declared
    owners:
      - packages/records
  - id: RECORDS-CONNECT
    category: records
    description: Salesforce export/sync/cursor/cutover handle deltas, retries,
      conflicts, and provenance
    verification: fake connector + DB
    disposition: declared
    owners:
      - packages/records
  - id: RECORDS-IDENTITY
    category: records
    description: Imported users reconcile/link/conflict/ignore and backfill
      ownership without scope escalation
    verification: identity state oracle
    disposition: declared
    owners:
      - packages/records
  - id: RECORDS-WATCH
    category: records
    description: Starter watches bind/reconcile and enqueue evaluation exactly once
      per source event
    verification: event/receipt oracle
    disposition: declared
    owners:
      - packages/records
  - id: RECORDS-ACTION
    category: records
    description: Records action execution obeys policy and produces the intended
      record mutation/receipt
    verification: action + state oracle
    disposition: eval
    owners:
      - packages/records
  - id: RECORDS-OPS
    category: records
    description: Health, lifecycle, system findings, backup verification, and ops
      watches report real conditions
    verification: fault-injected smoke
    disposition: declared
    owners:
      - packages/records
  - id: SEC-PRINCIPAL
    category: security-governance
    description: Human/service identity and backend actor are snapshotted and
      propagated end to end
    verification: trace + audit oracle
    disposition: declared
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: SEC-TENANT
    category: security-governance
    description: Org boundaries hold across data, records, threads, memory, files,
      actions, channels, and telemetry
    verification: cross-tenant adversarial
    disposition: eval
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: SEC-AUTHZ
    category: security-governance
    description: Admin/member/service and source roles allow exactly their declared
      operations
    verification: capability matrix
    disposition: declared
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: SEC-TOOL-ALLOWLIST
    category: security-governance
    description: Backend/operator-local tools outside the run catalog are unreachable
    verification: tool inventory + attack
    disposition: declared
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: SEC-EGRESS
    category: security-governance
    description: Undeclared network access is denied and emitted as a structured
      capability denial
    verification: sandbox E2E
    disposition: declared
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: SEC-SECRETS
    category: security-governance
    description: Keys stay outside sandboxes/model context and are scrubbed from
      output, logs, telemetry, and artifacts
    verification: canary secret scan
    disposition: declared
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: SEC-PROMPT-INJECTION
    category: security-governance
    description: Untrusted data/tool content cannot override data, action, identity,
      or exfiltration policy
    verification: adversarial corpus
    disposition: eval
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: SEC-APPROVAL
    category: security-governance
    description: Consequential changes cannot bypass proposal/policy/approval paths
    verification: negative side-effect oracle
    disposition: eval
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: SEC-AUDIT-CHAIN
    category: security-governance
    description: Append-only chain sequence/hash verifies and exposes logging
      failure independently
    verification: tamper test
    disposition: eval
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: SEC-BEHAVIOR
    category: security-governance
    description: Excessive action/memory/control-plane rates raise the expected
      alert without false cross-org attribution
    verification: fake clock + DB
    disposition: declared
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: SEC-DEPLOYMENT-PROFILE
    category: security-governance
    description: Solo/team/org/hardened defaults change controls coherently while
      the always-on floor remains
    verification: profile matrix
    disposition: declared
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: SEC-TELEMETRY
    category: security-governance
    description: Content mode, redaction, cardinality, tenant hashing, retention,
      and export controls are enforced
    verification: telemetry conformance
    disposition: declared
    owners:
      - packages/llm/src/work
      - packages/records/src/policy
  - id: OPS-JOB-LIFECYCLE
    category: operability-recovery
    description: Queue, running, retrying, succeeded, failed, and cancelled states
      are monotonic and attributable
    verification: job state oracle
    disposition: declared
    owners:
      - apps/worker/src
      - apps/openneko/internal
  - id: OPS-RECONCILE
    category: operability-recovery
    description: Lost worker/process state is reconciled without duplicate effects
      or permanently stuck runs
    verification: crash/restart E2E
    disposition: declared
    owners:
      - apps/worker/src
      - apps/openneko/internal
  - id: OPS-STARTUP
    category: operability-recovery
    description: Migrations, database, GraphJin, gateway, worker, web, and sandbox
      dependencies start in a valid order
    verification: packaged smoke
    disposition: declared
    owners:
      - apps/worker/src
      - apps/openneko/internal
  - id: OPS-PROVISION
    category: operability-recovery
    description: Provider, model, source, sandbox, and credential configuration
      reaches every required process consistently
    verification: deployment fixture
    disposition: declared
    owners:
      - apps/worker/src
      - apps/openneko/internal
  - id: OPS-HEALTH
    category: operability-recovery
    description: Status distinguishes healthy, degraded, transient, and terminal
      failures with actionable causes
    verification: fault-injected smoke
    disposition: declared
    owners:
      - apps/worker/src
      - apps/openneko/internal
  - id: OPS-DEMO
    category: operability-recovery
    description: Demo seed, simulator, and scenarios are reproducible and resettable
    verification: snapshot/digest smoke
    disposition: declared
    owners:
      - apps/worker/src
      - apps/openneko/internal
  - id: OPS-BACKUP
    category: operability-recovery
    description: Records backup and verification detect corruption/missing data and
      report safely
    verification: backup restore test
    disposition: declared
    owners:
      - apps/worker/src
      - apps/openneko/internal
  - id: OPS-UPGRADE
    category: operability-recovery
    description: Config/schema/plugin/runtime upgrades preserve supported state and
      fail safely
    verification: upgrade matrix
    disposition: declared
    owners:
      - apps/worker/src
      - apps/openneko/internal
  - id: OPS-RESOURCE
    category: operability-recovery
    description: Disk, memory, network, provider quota, and sandbox limits degrade
      predictably and remain observable
    verification: soak/fault injection
    disposition: declared
    owners:
      - apps/worker/src
      - apps/openneko/internal
